import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function LessonsScreen() {
  return (
    <PlaceholderScreen
      title="Lessons"
      description="Published lesson catalog will load from the local store, then the API."
    />
  );
}
