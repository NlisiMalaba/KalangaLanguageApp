# Kalanga mobile

Expo SDK 54 + Expo Router client. Talks to the .NET API, not Supabase.

```bash
npm install
npx expo start
npm test
```

Set `EXPO_PUBLIC_API_BASE_URL` (see `.env.example`). JWTs and password hashes go in expo-secure-store only.
